function toggleMenu() {
    var menuBox = document.querySelector('.toggle-menu');
    var menu = document.querySelector('.nav-ul')
      menu.classList.toggle('show');
      menuBox.classList.toggle('show');
}
